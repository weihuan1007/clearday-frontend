window.CLEARDAY_CONFIG = {
  // Use /api when CloudFront routes /api/* to the EC2 backend.
  // Use a full URL, such as "https://your-api.example.com/api", when the API is hosted separately.
  apiBase: "/api",
};
